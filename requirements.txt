# PyTorch (CPU only, latest available)
torch==2.7.1
torchvision==0.20.1
torchaudio==2.7.1

# TensorFlow (CPU only)
tensorflow==2.15.0
tf-keras==2.15.0  # avoids keras 3 issue

# Transformers (NLP)
transformers==4.45.2
huggingface-hub==0.24.6

# JAX (use compatible versions)
jax==0.4.28
jaxlib==0.4.28

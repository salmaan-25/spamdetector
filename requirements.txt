# Hugging Face + Deep Learning stack (Python 3.12 compatible)

# PyTorch (CPU only – works everywhere)
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2

# TensorFlow (CPU only)
tensorflow==2.15.0
tf-keras==2.15.0  # compatibility layer instead of keras 3

# Transformers (NLP)
transformers==4.41.2
huggingface-hub==0.23.0

# JAX (compatible versions)
jax==0.4.25
jaxlib==0.4.25
